# 動動腦小專案

一個簡單的益智問答網頁遊戲，不需要安裝套件，直接用瀏覽器打開 `index.html` 就能玩。

## 內容

- 10 題腦力題
- 倒數計時
- 分數與連對統計
- 手機與電腦都能使用

## 使用方式

直接開啟 `index.html`。

如果要放到 GitHub Pages，可以把這個專案推到 GitHub，然後在 repo 的 Pages 設定選擇 `main` 分支。
